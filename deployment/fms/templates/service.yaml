apiVersion: v1
kind: Service
metadata:
  name: {{ include "fms.fullname" . }}
  labels:
    {{- include "fms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fms.selectorLabels" . | nindent 4 }}
